get_file_list
Во всех каталогах внутри переменной disc_path   находим все файлы с типом  file_type.
Для них вычисляем md5.
В файл out_file_name пишем такие случаи, когда для одного хеша есть более одного файла(один и тот же файл с
разными названиями).

delete_double_files
Удаляет данные из файла с именем out_file_name